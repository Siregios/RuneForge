using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class WorkOrderPage : MonoBehaviour { 
    public GameObject minigameLine; //A panel with a check icon (Image), name of the minigame (Text), and score (Text)
    WorkOrder order;
    Text orderText;
    Image orderIcon;

    
}